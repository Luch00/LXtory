<Window x:Class="ScreenShotterWPF.EncodingProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:prism="http://prismlibrary.com/"
        prism:ViewModelLocator.AutoWireViewModel="True"
        xmlns:local="clr-namespace:ScreenShotterWPF"
        mc:Ignorable="d"
        Title="Encoding Gif.." Height="70" Width="335" ResizeMode="NoResize" WindowStartupLocation="CenterScreen" Icon="Resources/meh.ico">
    <Window.TaskbarItemInfo>
        <TaskbarItemInfo ProgressValue="{Binding ProgressValue}"/>
    </Window.TaskbarItemInfo>
    <Grid>
        <StackPanel Orientation="Horizontal">
            <ProgressBar Name="encodingProgress" Value="{Binding ProgressValue}" Width="250" Margin="4"/>
            <Button Name="btnCancel" Margin="8,4,4,4" Width="50" Click="btnCancel_Click" Command="{Binding CancelCommand}">Cancel</Button>
        </StackPanel>
    </Grid>
</Window>
